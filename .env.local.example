# In production, set NEXT_PUBLIC_API_BASE_URL to your backend origin (e.g., https://api.cosmicdharma.app).
# Base URL of FastAPI (root, WITHOUT /api suffix)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_MAPBOX_TOKEN= # optional; if set we use Mapbox Geocoding